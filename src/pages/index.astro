---
export const prerender = false;

const locale = Astro.preferredLocale ?? "en";

console.log("Preferred locale:", Astro.preferredLocale);
console.log("Accept-Language header:", Astro.request.headers.get("Accept-Language"));

return Astro.redirect(`/${locale}/`, 307);
---
